# 레슨의 모양 — 강의 한 구간의 뼈대

> 화자가 직접 말하는 오프라인 강의의 레슨 기준이다.
> 작업 진입점: `cd deck && npm run where -- chNN` → `npm run lessons -- chNN`
> → `npm run shots -- --audit chNN` → `npm run lint:offline`.

> Status: 영상 제작 기준
> Role: 레슨 단위의 판단 기준.

## 0. 무엇을 판정하는가

목표는 학습자가 다음 판단을 해보고 싶고, 설명 뒤에는 그 판단을 할 수 있는 영상이다.
스텝·구도·길이·캡처는 그 경험을 점검할 단서다. 경고가 없어졌다고 영상이 좋아진 것은 아니다.

**유튜브식 연출을 기본 방향으로 삼는다.** 문제로 여는 도입, 화면의 변형, 근거로 전환하는
컷, 확대와 시선 이동, 짧은 착지는 오프라인에도 적용한다. 오프라인이라고 모션을 줄이거나
슬라이드식 낭독으로 되돌리지 않는다. 단 리듬의 주체는 화면이 아니라 화자다. 컷이 많다는
사실도 품질 점수가 아니다. 이 강의는 설명과 연결되는 모션·실제 증거·사례의 반전으로
화면의 변화를 만든다.

판정 순서는 **학습 목표 → 말과 그림의 일치 → 현장 리듬**이다.

## 1. 레슨 = 질문 하나를 끝내는 영상 한 편

- 한 레슨은 **독립된 질문 하나를 세우고 그 판단을 완결한다.** 짧아도 완결하면 유지하고,
  길어도 같은 과제의 행동과 결과가 계속 변하면 유지한다. 합치기·쪼개기를 길이만으로
  결정하지 않는다.
- 쪼갠 챕터는 파일 하나가 레슨 하나다 (`chapters/chNN/NN-LNN-*.ts`).
  챕터 여닫기는 이웃 레슨에 포함한다.
- 한 파일 챕터에서 `lessons`가 마커로 나눈 경계는 편집 후보다.
  두 경계가 자동으로 같다고 가정하지 않는다.
- 파일을 합치거나 나누면 파일명·인덱스의 레슨 목록·대본 순서를 함께 확인한다. 인덱스가
  glob이면 파일명 정렬을, 명시적 import이면 import/spread 순서도 확인한다.

## 2. 레슨의 다섯 박자

| 박자 | 학습자에게 생기는 일 | 화면의 역할 |
| --- | --- | --- |
| 여는 말 | 어떤 문제를 풀지 안다 | 질문·실물·결과로 보상을 보여 준다 |
| 개념 | 판단에 필요한 기준을 얻는다 | 용어와 구조를 연결한다 |
| 정점 | 핵심 관계가 보인다 | 이 레슨에서 가장 중요한 변형·비교·실행 |
| 사례·반례 | 조건이 바뀌면 답도 달라짐을 본다 | 새 입력·실패·경계·결과 |
| 회상·착지 | 기준을 직접 적용하고 판단을 남긴다 | 선택 → 정답/근거 → 판단 한 줄 |

정점이 될 그림이나 행동을 먼저 정하고 앞뒤를 구성한다. 모든 레슨을 같은 시각표에 맞추거나
박자 다섯을 억지로 채우지 않는다. 스텝의 대본 예산은 VIDEO-PACING-GUIDELINES.md §0-1의
글·초점·의미·풍부한 의미 네 강도를 따른다.

## 3. 구도 — DOM 유지와 화면 교체는 다르다

구도는 **시청자가 보는 주된 그림과 공간 관계**다. `sceneGroup`은 React DOM을 유지할 구현
단위다. 같은 DOM 안에서도 사다리 → 비교판 → 평가판 → 순환도로 바뀔 수 있다. 같은 벤더
필름이라는 이유만으로 한 구도로 합치지 않는다.

- 이름표·강조색·카메라 배율만 바뀌면 구도를 새로 세지 않는다.
- 다른 증거·공간 관계·행동이 나타나면 같은 DOM 안에서도 새 구도일 수 있다.
- 한 구도가 오래 서 있으면 계속 진화하는 그림인지, 이미 읽은 그림을 붙잡고 있는지 본다.
- 필요한 컷의 우선순위는 **관련된 실물/실행 → 새 관계를 그린 도형 → 짧은 글판**이다.
  실물 없이 도형으로 설명이 완결되면 그대로 둔다.
- 글판 컷은 짧은 쉼표다. 긴 비교 카드를 끼워 구도 수만 늘리지 않는다.
- 연속 동작 한가운데 다른 레이아웃을 끼우면 재마운트로 흐름이 끊긴다. 반면 **한 설명을
  마친 뒤 근거를 보고 돌아오는 의도적 컷**은 가능하다. 복귀할 때 무엇을 유지·재소개하는지
  정하고 `shots --ghost`와 실제 전환을 확인한다.
- `sceneCut: true`는 **제작자의 변형 선언**이다. 새 애니메이션을 만들거나 변형을 검증해
  주는 속성이 아니다. 무엇이 어떻게 변형되는지 실제 상태를 확인한 뒤 사용한다.

`lessons --runs`가 표시하는 구도의 근거: `renderer`는 필름 World의 실제 mode·독립 판 공개
분기에서 계산한 것, `inferred`는 사진 경로·묶음 규칙으로 근사한 것, `declared`는 `sceneCut`
선언을 따른 것이다. 셋 다 렌더 검수의 대체물이 아니다.

## 4. 자료는 개수보다 역할

매 레슨 실물을 의무로 채우지 않는다. `캡처/사진 0초`는 재고 정보이며 오류가 아니다.

| 자료 | 확인할 역할 |
| --- | --- |
| 공식 문서 캡처 | 방금 주장한 내용의 그 줄을 확인하는가 |
| 실제 입력·화면 녹화·Trace·결과 | 어떤 행동이 어떤 결과를 만들었는가 |
| 그린 사례·구조도 | 조건·관계·판단을 더 잘 이해하게 하는가 |
| 사진 | 이 사례와 질문을 구체화하는가 |

공식 문서의 캡처가 곧 도구를 직접 사용한 증거는 아니다. 도구는 파일의 종류를 셀 뿐,
자료의 진위나 교육적 역할을 인증하지 않는다. 재고는 `deck/public/shots/`. 로그인·계정이
필요한 캡처는 사용자에게 필요한 화면과 부분을 구체적으로 전달한다.

## 5. 첫 15초와 회상·착지

첫 15초에 이 편의 질문과 얻을 판단이 보이게 한다. 번호·인사·진행 안내만으로 시작하면
검토한다. 앞 편을 받는 문장 자체는 금지가 아니며 이 편의 문제를 먼저 세운다.

마지막 두 장 안에는 학습자가 답할 행동을 둔다.

- 새 사례를 주고 선택·분류·진단·예측 중 하나를 요구한다.
- 오프라인은 화면을 멈추지 않고 청중에게 직접 묻는다. 답을 들은 뒤 정답과 이유를 공개한다.
- 강사가 스스로 묻고 같은 문장에서 답한 것은 회상이 아니다.
- “잘 멈추는 것도 성공”은 설명이다. ‘멈추’나 물음표의 존재로 회상을 판정하지 않는다.
- 마지막에는 이 레슨의 판단을 남긴다. 삭제한 뒤 챕터 예고는 복원하지 않는다.

`lessons`는 **선택 지시와 다음 답변 스텝을 찾은 후보**만 표시한다. 후보의 화면 ID·질문
스텝·정답 스텝을 함께 읽고, 질문의 난이도와 정답의 조기 노출은 렌더로 확인한다. 후보
없음은 수동 검토할 곳이지 회상이 없다는 증명이 아니다.

## 6. 리듬의 대비

레슨 안의 스텝이 전부 비슷한 길이로 흐르면 긴장과 보상이 생기지 않는다. 반복된 착지·
진행 안내를 줄이고, 정점에서 학습자가 처리할 시간을 남긴다. 대비를 만들려고 긴 스텝을
새로 넣거나 필요한 설명을 자르지 않는다.

## 7. 화면 글·음성

제목은 결론, 말은 이유를 담당한다. 긴 문장을 제목과 중앙 설명에서 동시에 읽히지
않는다. 기준은 "화자가 말하는 동안 청중이 볼 곳이 하나인가"다.

같은 제목이 레슨 안에서 반복되면 검토한다(커버·챕터·인용은 예외). 도구의 `제목 반복
후보`는 문자열 일치이므로 같은 문장을 낭독하는지 직접 읽는다.

## 8. 기술 변경의 범위

기존 레이아웃의 스텝·초점·순차 공개와 대본은 작업 범위에 맞게 고친다.

금지: 장식 컷으로 수치만 맞추기, 카메라만 미는 비트를 의미 변형으로 세기, 비운 ID 재사용,
기각된 경고를 그대로 재상정하기, 뒤 챕터 예고 복원. 사용자의 현재 수정 범위가 모든 일반
절차보다 우선한다.

## 9. 재는 순서

`where`로 작업 대상의 번호·ID·파일을 풀고, `lessons`로 레슨의 질문·구도·자료를
읽고, `shots --audit`로 실제 그림의 깨짐을 확인한다. 수정 중 `lint:offline`은
빠른 정합성 검사다. 대표 장면과 새 연출은 `shots` PNG를 눈으로 확인한다.

```bash
cd deck
npm run where -- ch04
npm run lessons -- ch04
npm run shots -- --audit ch04 --slides 14-19
npm run lint:offline
```

## 10. 본보기

| 본보기 | 옮겨 배울 판단 | 그대로 복제하지 않을 것 |
| --- | --- | --- |
| 회상과 종합 사례 · `workflow-ask` | 설명한 기준을 다른 입력에 적용하고, 같은 사례에서 관계를 변형한다 | 질문판의 외형만 붙여 회상이 있다고 판정하기 |
| 전체 폭 서사·용어 정착 · `trace-timeline` | 같은 사례가 여러 판단으로 진전되고, 전체에서 한 단계로 들어간다 | 같은 장면 수·대본 길이를 맞추기 |
| 상태 변화·운영 판단 · `openai-recall` | 값·조건·결과가 기존 공간에서 변하며, 배운 기준을 새 사례에 적용한다 | 3D·카메라·발광을 모든 주제에 사용하기 |

이 덱의 본보기는 `open-take`(연속 장면) · `token-ticker`(숫자 연출) ·
`factory-loop`(순환도)다. 새 연출은 이 셋의 **연속 장면 + 대본 + 앞뒤 맥락**과
비교한다. 정지 이미지 한 장으로 대신하지 않는다.

특정 레슨의 과거 초·구도 수를 고정 기준으로 복사하지 않는다. 다시 측정한 결과가 정본이고,
선언 수정과 실제 화면 개선을 전후 보고에서 구분한다. 현재 챕터 상태는 작업 메모리의
최신 기록을 따른다.

## 11. 설계·제작·비평·재작업

챕터 종합 개편에 적용한다. 작은 문구·화면 수정은 해당 단계만 수행한다. 여러 단계마다
사용자 승인을 요구하지 않는다. 사용자에게 처음 넘기는 결과 안에 내부 반복을 포함한다.

1. **비교 기준을 고른다.** §10에서 설명 역할이 맞는 완성 사례를 직접 읽는다. 사용자에게
   본보기를 다시 선정해 달라고 하지 않는다. 사실·출처의 기존 검증은 재사용한다.
2. **챕터의 이해 흐름을 먼저 그린다.** 레슨별 시작 질문·이미 아는 것·핵심 오해·정점의
   사건·마지막에 할 판단을 적는다. 현재 장표 목록을 그대로 목차로 옮기는 것은 설계가 아니다.
   본문을 자르기 전에 같은 사례로 이어갈 구간과 조건을 바꿔 재적용할 구간을 정한다.
3. **대표 레슨을 끝까지 완성한다.** 성격이 다른 두 레슨 정도를 고른다(한 레슨 요청이면 한 편).
   추상 개념을 정착시키는 편과 실행·실패를 따라가는 편처럼 서로 다른 설명 부담을 포함한다.
   막히는 핵심 구간에는 서로 다른 전개안을 비교하고 이유를 남긴 뒤 구현한다. 모든 장표의
   대안을 의무적으로 만들거나 예쁜 오프닝 몇 장으로 전체 방향을 검증하지 않는다.
4. **제작 의도를 가리고 비평한다.** 완성 사례와 이번 대본·화면을 먼저 보고 §12를 적용한다.
   비평마다 화면 ID/스텝·대본 구절·학습자가 막히는 이유·수정 방향을 붙인다.
   ‘역동적으로’, ‘85점’처럼 근거 없는 평가를 재작업 지시로 쓰지 않는다.
5. **고치고 다시 확인한 뒤 확장한다.** 주요 결함을 발견만 하고 넘기지 않는다. 재작업에서
   무엇이 해결되었는지 해당 대본과 실제 화면으로 대조한다. 이후 전체 레슨에 확장하고
   챕터를 연속으로 읽어 용어 선행·누락·반복 전개를 점검한다. 같은 틀을 전 장표에 씌우지 않는다.

대상 챕터의 기존 리뷰에 다음을 기록한다. 별도 날짜별 리뷰나 점수표를 늘리지 않는다.

| 기록 | 필요한 근거 |
| --- | --- |
| 설계 | 레슨 질문·선행 이해·오해·정점의 실제 사건·적용 판단, 본보기와 선택 이유 |
| 대표 레슨 | 선택한 ID와 이유, 대본 및 실제로 확인한 화면 상태 |
| 비평과 수정 | 문제 ID/스텝 → 이해가 막힌 이유 → 수정 → 재확인 결과 또는 미해결 |
| 납품 범위 | 읽은 대본 범위·본 화면 범위·기술 검사 모드·사용자에게 남긴 판단 |

**첫 납품 조건:** 설명을 따라가는 데 필요한 개념·사례·화면 대응의 주요 누락을 해결했고,
대부분의 레슨에서 사용자가 다시 내용·연출 아이디어를 공급할 필요가 없어야 한다.
대본을 늘린 구간과 새 연출은 **실제 또는 추정 낭독 시간 동안의 화면**을 확인한다.
처음·중간·끝 스텝의 정지 이미지와 전환 적용 검사만으로 이를 대신하지 않는다.
문구 추가·교체 뒤 긴 정지 체류가 남으면 VIDEO-PACING-GUIDELINES §0-1·0-2·10에 따라
고친 뒤 넘긴다. 리뷰에는 해당 ID/스텝의 말하기 시간, 등장 이후 이어지는 시각적 진행 또는
정지 유지의 구체적 이유를 남긴다. 자동 검사를 통과해도 납품 조건을 충족하지 않는다.
완성 사례에 비해 부족한 구간은 재작업한다. 자동 경고 수로 이를 대체하지 않는다.
해결이 막히면 수행한 대안과 남은 문제를 명시한다. 미완성을 승인 요청이나 완료로 포장하지 않는다.
취향 선택·미확인 사실은 판단할 이유와 함께 남긴다.

## 12. 처음 듣는 사람이 따라오는가

문장을 매끈하게 만드는 것보다 **학습자가 다음 설명을 이해할 준비가 되었는지** 본다.
이 평가는 에이전트의 교육 설계 검토이며 실제 초심자 학습 실험을 했다는 뜻은 아니다.

- **선행 이해:** 지금 필요한 용어·관계가 앞에서 자리 잡았는가. 앞 챕터에서 설명했다는
  사실만으로 생략하지 말고, 지금 장면을 이해하는 데 필요한 맥락을 짧게 회수한다.
- **구체화:** 정의의 명사들이 같은 사례의 사람·요청·문서·행동 중 무엇인지 짚을 수 있는가.
  주의사항만 이어져 핵심 동작을 한 번도 따라가지 못하는 구간은 다시 구성한다.
- **설명의 진전:** 주장 → 왜 그런지 → 실제 진행과 결과가 이어지는가. 중요한 구분은
  조건을 바꾼 사례에서 재등장시키고, 시청자가 직접 적용할 기회를 둔다. 횟수 할당은 하지 않는다.
- **반복의 역할:** 같은 뜻을 다시 읽는 문장은 줄이되, 새로운 조건·관점·예외·판단으로
  이해를 쌓는 반복은 보존한다. 핵심을 한 번 언급했다는 이유로 학습이 끝났다고 판정하지 않는다.
- **그림과 청취:** 대본의 ‘이것·그 경우·앞에서’가 무엇인지 그 상태에서 알 수 있는가.
  화면에 처음 등장하는 용어·숫자·관계를 설명 없이 동시에 요구하지 않는가.
- **적용:** 마지막 문제의 정답을 고르는 근거를 앞 설명에서 배웠는가. 강사의 상식을
  알아맞히거나, 정답 색·긴 선택지 같은 단서로 맞히는 문제는 고친다.
- **연속 시청:** 장표별 표현을 바꾸어도 ‘문제 둘 → 처방 하나’ 같은 전개가 계속되는가.
  사건 추적·비교·원인 역추적·조건 변경 등 내용에 맞는 흐름을 선택한다. 회상처럼 동일한
  형식이 학습 행동의 신호가 되는 부분은 유지한다.

ch04의 현재 우선순위는 **개선된 디자인 문법을 보존하며 초심자의 이해를 보강**하는 것이다.
예를 들어 Plugin은 같은 업무 묶음의 구성 → 설치 → 계정 인증이 별도로 필요한 상황 →
다른 조건에 적용하는 판단으로 설명을 잇는 안을 검토할 수 있다. 이는 제작 예시이며 모든
레슨에 적용할 고정 순서가 아니다. 실제 제품의 지원 범위는 기존 검증 근거를 따른다.
