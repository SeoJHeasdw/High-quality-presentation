# 강의 화면 리듬 가이드

> 화자가 직접 말하는 오프라인 강의의 화면 기준이다.
> 움직임의 주체는 화면이 아니라 화자다. 화면은 매초를 채우지 않고 박자를 만든다.
> 시간 한도는 글 30초 · 초점 36초 · 의미 45초다 (`lint:offline`).
> 레슨 단위 판단은 `LESSON-SHAPE.md`, 구현 함정은 `docs/authoring-rules.md`.

## 0. 가장 중요한 원칙

관리할 것은 슬라이드 파일의 개수가 아니라 **시청자가 실제로 인지하는 시각 변화**다.

연출 방향은 유튜브식 문제 도입·장면 변형·근거 컷·시선 이동을 적극 활용한다.
Udemy에 올린다는 이유로 모션을 줄이거나 발표 슬라이드로 되돌리지 않는다.
화려함은 이해를 돕는 변화로 만들고, 컷 수 자체를 품질로 평가하지 않는다.

| 단위 | 뜻 | 예시 |
| --- | --- | --- |
| 논리 화면 | `id` 하나를 가진 강의 장면 | `promptify-check-1` |
| 나레이션 스텝 | 대본의 `### 0`, `### 1` | 음성 클립과 화면 진행의 기준 |
| 시각 비트 | 시청자가 새 정보나 새 초점으로 인지하는 상태 | 목록 한 항목 공개, 좌→우 강조 이동 |
| 체감 장면 | 지배적인 이미지·구도·공간 관계가 같은 동안의 묶음 | 같은 사진 위에서 질문·정답·문구만 바뀌는 구간 |

논리 화면 하나가 길어도 내부에 의미 있는 시각 비트가 이어지면 괜찮다. 반대로 배경만
미세하게 움직이는 한 줄 장표는 논리 화면이 하나여도 **그냥 정지 화면**이다.

판정 순서는 **이해 → 화면·음성의 동기화 → 리듬**이다. 학습자가 생각할 시간을 없애거나
이해와 무관한 컷을 추가하지 않는다. 앰비언트 배경·글로우·입자·반복 루프만 바뀌는 것은
새 비트가 아니다.

종합 개편은 `LESSON-SHAPE.md` §11~12의 설계·비평·재작업과 초심자 기준을 먼저 적용한다.
이 문서의 작은 수정 기법을 기존 전개의 유지 명령으로 해석하지 않는다. 디자인과 내용이
이미 승인된 구간의 영상미 보강은 그 승인 범위를 지키고, 이해 부족이 지적된 구간은
설명 순서·사례·재적용 기회까지 고친다. 초 단위 수치를 맞추려고 필요한 재설명을 없애지 않는다.

### 0-1. 한 스텝의 대본 예산은 변화 강도로 정한다

스텝 수가 같아도 화면이 받는 대본의 양은 같지 않다. **텍스트 한 줄이 더 보이는 스텝과
구조 자체가 바뀌는 스텝을 같은 기준으로 재지 않는다.** 강도는 네 단계로 구분한다.

| 변화 | 무엇이 바뀌었나 | 받을 수 있는 대본 |
| --- | --- | --- |
| **글** | 문장·라벨·착지 한 줄이 추가됨 | 가장 짧다. 쉼표 역할 |
| **초점** | 같은 그림에서 말하는 곳이 나오고 나머지가 물러남 | 시선 경로가 유지되는 동안 |
| **의미** | 새 구조·관계·결과·행동이 보임 | 정보가 실제로 생기는 동안 |
| **풍부한 의미** | 여러 정보가 순차로 생기고 시선이 계속 이동함 | 가장 길게 버틴다 |

판정은 **스텝 진입 순간이 아니라 진입 애니메이션이 끝난 뒤의 상태**로 한다. 카드가
0.7초 작아진 뒤 멈춰 있으면 그건 애니메이션 장면이 아니라 0.7초 변화 뒤에 붙은 정지 화면이다.

- 글 스텝이 길어지면 같은 뜻만 되읽는 문장부터 검토한다. 처음 듣는 사람이 따라오려면
  필요한 구체화·조건 변경·재적용은 남기고, 이를 설명하는 그림과 상태를 함께 설계한다.
- 초점 스텝이 길어지면 확대·디밍·연결선처럼 **말하는 동안 유지되는 시선 경로**를 만든다.
- 의미 스텝이라도 새 정보가 1초 안에 다 뜨면 나머지 시간은 완성 상태의 강도로 다시 판정한다.
  필요하면 항목을 순차 공개한다.
- 호흡하는 글로우·느린 확대·앰비언트 루프는 화면을 살아 보이게 할 수는 있어도
  대본 예산을 늘려 주지 않는다.
- 여러 칸·행·관계가 실제로 순차 변화하는 긴 스텝은 억지로 자르지 않는다.
  정보 밀도가 그 시간을 받는지가 기준이다.

> 오프라인: 위 강도는 같되 대본 예산이 3배다. 글 스텝도 화자의 호흡이 받치면
> 30초까지 간다. 기준은 "화자가 말하는 동안 청중이 볼 곳이 있는가" 하나다.
> 화면이 가만있어도 화자가 움직이면 정지 화면이 아니다.

### 0-2. 움직임은 시간을 벌어 주지 않는다

움직임은 반복·경로·대기·비교처럼 **현재 설명의 뜻을 보이게 할 때** 쓴다. 새 정보를 주지
않는 호흡·입자·글로우는 정지감을 조금 완화할 뿐이고, 학습과 무관한 움직임은 오히려
시각 채널의 인지 부하를 늘린다. 한 상태가 길다는 이유만으로 무한 애니메이션을 추가하지
않는다. 우선순위는 **내용의 필요성 확인 → 설명을 따라가는 초점·관계 변화 → 필요한 보조 모션**이다.

**내용과 길이가 승인되고 영상미 보강을 요청받은 구간은 대본 단축이나 기존 3D 장면의
평면 카드 대체를 기본 해법으로 삼지 않는다.** 기존 그림 안에서 설명이 진행되게 만들고,
남은 정적인 체류에는 §10의 작은 효과를 선택적으로 쓴다. 벤더·관점이 달라 맥락을 다시
쌓는 반복도 단순 중복으로 자동 삭제하지 않는다.

- 구조·관계·초점이 바뀌는 움직임은 새 시각 비트가 될 수 있다. 장식 움직임은 아니다.
- 진입할 때 0.5초 움직이고 멈추는 것은 긴 대본을 받쳐 주지 못한다. 설명 중 확인할
  대상에 변화가 이어져야 한다. 한 번 끝난 업무의 실행을 계속 반복시키지 말고,
  완료 상태를 남기거나 읽기 강조를 이어간다.
- 가능하면 대본의 문법을 따른다. 반복을 말하면 신호가 고리를 돌고, 경로를 말하면 빛이
  선을 따라가며, 비교를 말하면 초점이 양쪽 사이를 이동한다.
- **아직 대본이 소개하지 않은 정보 요소를 먼저 움직이지 않는다.** 흐리게 예고한 카드·
  숫자·경로가 스스로 살아나면 청중은 그 내용을 설명 중이라고 오해한다. 이때는 제목 강조,
  배경의 빛처럼 현재 말과 충돌하지 않는 곳에서 호흡을 만든다.
- 짧은 정의·결론이나 명시적인 생각 시간처럼 정지할 이유가 있는 상태는 유지할 수 있다.
  오프라인은 화자의 말이 곧 움직임이므로, 결론 한 줄 화면에 1분 가까이 머무는 것도
  정상이다. 재작업 대상은 "화자도 화면도 멈춘" 구간뿐이다.
  **문구 한 줄의 추가·교체와 짧은 페이드만 있는 긴 스텝은 재작업한다.** 설명의 진행에
  맞춰 스텝을 나누거나, 기존 대상 안에서 순차 변화·비교·읽기 강조를 이어가거나,
  불필요한 대본을 줄인다. 초심자에게 필요한 설명을 유지하면 그 시간을 받을 화면도
  함께 보강해야 한다. 장식 모션 금지는 정지감 개선을 생략하는 예외가 아니다.
- 같은 코드·파일명에 밑줄을 순환시키는 효과가 여러 개여도, 다른 대본이 말하는 새 근거·
  조건·기록이 화면에 생긴 것은 아니다. 제목·하단 한 줄만 바뀐 긴 상태를 읽기 효과의
  대상 수로 면제하지 않는다. 현재 대본이 설명하는 대상과 실제로 바뀐 대상을 대조한다.

긴 대본이 분기·반복·조건을 설명하는데 화면은 직선 칸인 채 화살표 색과 결론 한 줄만
즉시 바뀐다면 반대 사례다. 첫 녹화 프레임에서 이미 최종 상태로 멈춰 이후 음성 구간에
시간의 흐름이 없기 때문이다. **예쁜 최종 상태와 좋은 영상은 같은 것이 아니다.**

**읽기 선행을 확인한다.** 화면을 본 사람이 몇 초 만에 요지를 다 이해했는데 음성이 같은
뜻을 계속 풀고 있다면, 말속도를 올리지 말고 대본을 줄인다. 반복 문장은 새 근거·예외·
결과 중 하나를 추가할 때만 남긴다. 이것이 레슨 크기로 일어나면, 스텝 시계를 다 통과해도
시청자에게는 그림 하나짜리 정지 영상이다.

### 0-3. 첫 30초와 레슨 오프닝

첫 30초는 제목·썸네일의 약속을 내용이 바로 이행하는지 검증받는 구간이다. 인사말, 로고,
강사 이력, 긴 목차로 시작하지 않는다.

| 구간 | 해야 할 일 | 피할 것 |
| --- | --- | --- |
| 0~5초 | 시청자의 문제·결과·반전 중 하나를 즉시 보여 줌 | “안녕하세요”, 커버만 정지 |
| 5~15초 | 이 영상에서 얻게 될 판단이나 결과를 약속 | 추상적인 중요성 설명 |
| 15~30초 | 사례·데모·증거로 약속이 빈말이 아님을 확인 | 강사 소개, 전체 목차 낭독 |

- 커버는 플랫폼의 제목·썸네일을 다시 읽는 화면이 아니라 짧은 표식이다.
- 강사 신뢰는 이력 나열보다 실제 결과·코드·운영 경험·출처로 보여 준다.
- 각 레슨도 첫 15초 안에 `문제 → 이번 레슨의 보상`을 말한다.
- 첫 1분에 작은 글만 여러 번 여는 것은 통과가 아니다. 다만 컷 수를 채우기 위한
  장식 전환은 넣지 않는다.

## 1. 화면 유형별로 무엇이 변해야 하는가

| 화면 유형 | 필요한 변화 |
| --- | --- |
| 제목·전환 | 제목 진입 후 바로 다음 내용으로 이동 |
| 한 줄 결론·인용 | 핵심 단어 강조 후 사례나 근거로 전환 |
| 사진·스토리 | 질문, 확대·초점, 정답·해석을 순차 공개 |
| 목록·단계 | 읽는 항목만 공개하거나 강조 |
| 전후·좌우 비교 | 왼쪽 → 전환 → 오른쪽 순으로 시선 이동 |
| 표·매트릭스 | 설명 중인 셀만 밝히고 나머지는 낮춤 |
| 구조·다이어그램 | 노드·경로·원인과 결과를 순서대로 강조 |
| 제품 UI·실습 데모 | 클릭·입력·결과처럼 실제 화면 변화가 계속 존재 |
| 퀴즈·생각 시간 | 질문·선택·정답 공개를 구분. 필요하면 시청자가 일시정지 |

판정 질문은 하나다. **나레이션이 새 주장·증거·예시·행동으로 넘어갔는데 화면의 초점이 그대로인가.**

## 2. 오래 머물러도 되는 화면

시청자가 보고 따라갈 변화가 있으면 길어도 된다.

- 실제 제품을 조작하는 데모: 클릭·입력·선택·결과가 계속 보임
- 복잡한 다이어그램: 설명 순서에 따라 노드와 연결선이 차례로 활성화됨
- 코드·로그 분석: 현재 설명하는 줄이나 값이 계속 이동함
- 문제 풀이·퀴즈: 질문 상태, 생각 시간, 힌트, 정답 공개가 구분됨
- 감정적 여백이 필요한 장면: 의도적으로 멈추며 나레이션도 짧고 여백이 목적임

다음은 오래 머물러도 되는 근거가 **아니다.** WebGL 배경이 움직임 · 카드 테두리나 글로우가
반복됨 · 처음부터 모든 항목이 보이지만 화자가 하나씩 읽음 · 말만 계속 바뀜 ·
말하는 내용이 풍부하다는 이유만으로 화면은 그대로 둠.

## 3. 반드시 쪼개야 하는 화면

- 그림·근거 없는 한 줄 결론 화면에 대본이 계속 붙음
- 목록의 모든 항목이 처음부터 보이는 채로 설명이 이어짐
- 나레이션은 여러 사례를 말하는데 화면에는 사례가 반영되지 않음
- 비교 화면에서 양쪽을 동시에 보여준 채 시선 유도가 없음
- 같은 스크린샷을 설명 내내 그대로 보여줌
- 원인 → 과정 → 결과를 말하지만 완성된 도식 하나만 계속 보여줌
- 말해도 의미 있는 픽셀이 바뀌지 않음

## 4. 화면을 늘리는 일곱 가지 방법

새 슬라이드 ID를 만드는 것만이 해법은 아니다. 설명 구조가 적절한 구간은 아래 순서로
가장 작은 변화를 선택한다. 종합 개편에서 전개 자체가 부족하면 레슨 설계부터 다시 본다.

**① 순차 공개** — 목록·과정·사례를 한 번에 다 보여주지 않는다. 현재 말하는 항목을 공개하고
앞 항목은 남기되 대비를 낮춘다.

**② 초점 이동** — 같은 구조를 유지하면서 강조점만 옮긴다. 청중이 공간 구조를 다시
학습하지 않아도 된다.

**③ 확대·주석** — 사진·제품 화면을 유지하되 설명하는 부분을 확대하고 테두리·화살표·짧은
라벨을 붙인다. 의미 없는 줌만으로는 시각 비트가 되지 않는다.

**④ 사례 컷어웨이** — 추상적인 결론 뒤에 실제 문장·제품 화면·짧은 데모·결과를 붙인다.
한 줄 결론 화면이 길어질 때 가장 먼저 고려한다.

**⑤ 새 논리 화면** — 주장·증거·공간 구조가 달라지면 새 `id`를 만든다. 같은 구조에서
강조만 바뀌면 기존 화면의 스텝을 늘리는 편이 낫다.

**⑥ 중앙 포커스 비트 — 드물게만** — 복잡한 화면을 잠깐 물리고 **착지 한 문장만 중앙에
남긴다.** 다음 스텝에서 원래 화면으로 돌아오며 새 초점을 주면 새 `id` 없이도 컷이
들어간 것처럼 느껴진다.

- 기존 그림 안에서 초점을 만들 수 없을 때의 착지·전환·반전 한 문장에만 쓴다.
- **앞 장이나 다음 장에서 같은 연출을 쓰지 않는다.** 연속으로 나오면 집중 효과가 아니라
  반복 템플릿으로 보인다. 한 레슨의 결정적 착지에만 쓴다.
- 원래 화면은 숨기지 말고 어둡게 물려 앞 장면과의 연결을 남긴다. 뒤의 선·카드·텍스트가
  읽힐 정도로 남으면 실패다.
- 돌아온 스텝에서 **어디를 볼지 하나**를 분명히 한다. 원래 화면으로 돌아오기 전에 다음
  장으로 넘어간다면 포커스 비트가 아니라 별도 전환 장표이므로 쓰지 않는다.
- 중앙 문장이 길어지면 효과를 늘리지 말고 대본을 줄인다.

`promptify-both`처럼 기존 UI 안에서 메뉴 → 입력창으로 초점을 옮길 수 있는 화면에는 중앙
문장을 덮지 않는다. 이 경우에는 카드 사이 관계선이나 표 내부 행 강조처럼 **원래 그림의
문법 안에서** 변화를 만든다.

**⑦ 반례를 한 프레임 그렸다 되돌린다** — 대본이 부정형으로 말하는 문장("모든 단계에
붙이면 자동화가 아니다")은 말로만 지나가기 쉽다. 그 X를 한 스텝 동안 실제로 그려 놓고
다음 스텝에서 되돌리면 없던 사건이 둘 생긴다 — 그리는 순간과 되돌리는 순간.

- **새 요소를 띄우지 말고 색·상태를 뒤집는다.** 이미 있는 어휘로 그리므로 청중이 새
  그림을 배우지 않는다.
- **되돌아온 상태가 그 장표의 결론이어야 한다.** 되돌리지 않으면 반례가 주장으로 읽힌다.
- **반례 스텝에는 초점을 주지 않는다.** 전부가 주어라 물릴 곳이 없다.
- 한 레슨에 한 번이다. 두 번 쓰면 색이 뜻을 잃는다.

## 5. 대본과 화면을 맞추는 규칙

자동 렌더러는 나레이션 스텝이 끝날 때 `ArrowRight`를 보내 다음 상태로 진행한다. 설명의
큰 의미 전환은 스텝 경계와 맞추고, 같은 과정을 따라가는 작은 변화는 스텝 안에 배치한다.

- 새로운 주장·증거·예시·행동으로 넘어가면 스텝을 나눌 후보다.
- 한 스텝은 3~5문장(30~50초 말하기)으로 구성한다.
- 같은 값의 환산·조건 대입·자료 확인은 한 스텝 안에서 진행하고 결과를 남길 수 있다.
- 새 스텝을 만들었으면 컴포넌트가 그 스텝에서 실제로 다른 상태를 보여야 한다.
  **화면 상태가 바뀌지 않는 스텝은 버그다.** 단 오프라인에서 스텝을 안 나누는 것은
  버그가 아니다. 나누지 않은 것은 처음부터 한 상태로 설계한 것이다.
- 문장만 잘게 나누고 화면이 바뀌지 않으면 시각 비트가 늘어난 것이 아니다.

### 화면–대본 연결 검사

- 대본이 가리키는 명사와 숫자는 **그 스텝에 실제로 보여야 한다.** 점만 보이는 스텝에서
  모델 이름과 15조를 먼저 설명하면, 다음 스텝의 좋은 디자인도 값이 없다.
- 이전 장면을 그대로 이어 쓰는 첫 스텝은 장면 재사용 자체를 비트로 세지 않는다.
  `한 시간 뒤`처럼 라벨 하나만 바뀌면 대본을 줄이거나 새 초점을 만든다.
- 반복을 설명하는 화면은 진입 애니메이션 한 번이 아니라, 말이 흐르는 동안 고리·신호·
  경로가 계속 움직여야 한다.
- **뒷장에서 발견할 것을 앞장에서 미리 칠하지 않는다.** 결론 색(실패 행의 빨강, 고른 칸의
  테두리)을 앞 장에서 켜 두면 그걸 찾아내는 장은 **진입에 아무 사건이 없어** 밝기만 조금
  내려가는 장이 된다. 값은 데이터라 그대로 두고 **강조만 미룬다** — 상태 배지의 `ERROR`
  글자는 앞 장에도 있되 중립색이고, 붉어지는 건 그것을 짚는 장이다.

제작 순서: 대본의 의미 전환 표시 → 스텝 경계 결정 → 각 스텝의 시각 상태 설계 →
리허설 실측으로 최종 검수.

## 6. 검수 기준

검사 절차는 이 덱의 명령이 담당한다: `where`로 대상을 풀고 `lint:offline`으로
정합성을 보고 `shots --audit`로 깨짐을 본다. 아래는 리허설에서 눈과 귀로
판단할 기준이다.

**리듬**

- [ ] 진입 애니메이션이 끝난 뒤 남는 정지 시간을 별도로 확인함
- [ ] 오래 이어지는 상태는 반복 대본·읽기 선행을 검토했고, 필요한 경우에만 설명과
      관련된 순차 변화·신호가 있음
- [ ] 장식 움직임을 새 시각 비트나 긴 체류의 정당화 근거로 세지 않음
- [ ] 이전 장면을 재사용한 첫 스텝에도 새 초점·관계·정보 중 하나가 있음
- [ ] 대본이 말하는 명사·숫자가 같은 스텝의 화면에 실제로 보임
- [ ] 반복을 설명하는 고리·신호·경로가 대본이 흐르는 동안 계속 움직임
- [ ] 인접 장표에서 같은 중앙 포커스 연출을 반복하지 않음
- [ ] 글·라벨·강조만 바뀌는 동안 체감 장면이 새로 시작하지 않음을 알고 있음
- [ ] 장면 전환이 문장 중간이 아니라 의미 경계에 맞음

**영상성**

- [ ] 도형 없이 글만 이어지는 구간을 검토함. 설명이 추상어뿐이면 사례·증거·작동 장면
      중 하나로 전환하고, 의도적인 이야기 구간이면 예외 사유를 남김
- [ ] 같은 그림이 새 의미 없이 오래 서 있지 않음
- [ ] 글 비트가 화면 변화의 대부분을 차지하지 않음
- [ ] 목록은 **기억해야 할 목록**일 때만 씀. 설명해야 할 과정은 그림으로

**가독성·동기화**

- [ ] 한 순간에 가장 먼저 봐야 할 곳이 하나로 분명함
- [ ] 화면 텍스트가 발표자 말을 그대로 반복하지 않고, 말과 화면이 서로 다른 일을 함
  (화면은 결론, 말은 이유)
- [ ] 화면 변화가 말하는 문장의 끝과 맞고, 말이 끝난 뒤 어색한 정적이 없음
- [ ] 1배속 리허설에서 화면을 먼저 이해한 뒤 말을 기다리는 구간이 없음
- [ ] 답답한 구간은 말하기 속도보다 반복 대본과 체감 장면 길이를 먼저 줄임

**챕터 인계**

- [ ] `npm run build`와 해당 챕터 린터 오류 0
- [ ] 수정 범위의 `shots --audit`에서 `dead`·overflow·safe·overlap을 확인함
- [ ] 자동 검사를 통과해도 실제 영상에서 확인할 장표 번호와 스텝을 목록으로 남김

## 7. 도구가 띄우는 숫자를 읽는 법

린터와 `shots`는 대본 글자 수·상태 시간·무한 애니메이션 개수 같은 값을 띄운다. 전부
**검토할 자리를 찾는 진단값**이고 목표 점수가 아니다. 숫자를 맞추려고 장식 컷을 넣거나
필요한 설명을 자르지 않는다. 반대로 숫자가 깨끗하다고 영상이 좋아진 것도 아니다.

완성 영상을 직접 본 사람이 밀도·기술 설명의 필요성·의도적인 호흡을 근거로 특정 장표의
경고를 승인할 수 있다. 그때는 장표 데이터에 `pacingExceptions: ["desert"]`처럼 승인한
검사 종류를 적고, `pacingExceptionReason`에 이 장표에서만 예외가 성립하는 이유를 적는다.
이유 없는 예외는 콘텐츠 린터 오류다. 예외는 페이지 번호가 아니라 안정적인 화면 ID에
붙이고, 같은 레이아웃·주제·챕터의 다른 장표로 자동 전파되지 않는다. 완성 판정 이후에는
같은 경고를 다시 작업 목록에 올리지 않는다.

## 8. 스텝으로 될 화면과 쪼개야 할 화면

화면 하나에 붙은 **대본 전체 길이와 의미 전환**을 함께 본다. 린터의 길이는 검토할 구간을
찾는 값이며, 같은 사례가 진행되는 공간을 자동으로 나누는 기준이 아니다.

- 짧은 대본은 스텝으로 처리한다. 청중이 장면의 공간 구조를 다시 학습하지 않아도 된다.
- 다이어그램·연속 씬처럼 짚을 곳이 여러 개면 대본이 길어도 한 화면이 낫다.
- 대본이 길면 **주장·공간·과정의 진행을 검토한다.** 새 주장이나 새 공간이 필요하면 나누고,
  같은 사례의 값·조건·관계가 진행되면 기존 그림 안에서 이어간다.

**장 수를 늘리는 것이 목표가 아니다.** 실제로 새 `id`가 필요했던 자리는 늘 소수였고,
나머지는 그림이 스텝을 안 그리고 있었거나(레이아웃 문제), 카피가 통째로 서 있었거나,
대본이 안 쪼개져 있었다.

## 9. 화면을 새로 만들어야 하는 자리는 생각보다 적다

긴 화면을 만나면 **이 순서로** 따져 본다. 아래로 갈수록 비싸다.

1. **레이아웃이 스텝을 그리는가.** 대본을 `### 0 · ### 1`로 쪼개도 화면이 안 바뀌면 여기부터다.
   가장 많았던 원인이다.
2. **카피가 통째로 서 있는가.** 부제를 문장으로 나눠 하나씩 연다(`copy-steps.tsx`).
   `steps`는 부제 문장 수와 같게 준다.
3. **그림이 한 번에 다 서 있는가.** 칸·줄·막대를 하나씩 켠다. 말이 닿는 자리만 밝히는
   것도 한 비트다.
4. **대본이 말하는데 화면에 없는 것이 있는가.** 있으면 그리는 게 먼저다.
5. **서로 다른 주장이나 새 공간을 분리해야 하는가.** 그때 새 `id`를 만든다. 시간 초과만으로
   결정하지 않는다.

## 10. 뜻이 있는 움직임을 어디에 다는가

§0-2의 원칙을 실제 화면에 적용하는 규약이다. 지금 말하는 반복·경로·대기·비교를 보여주는
동작을 중심에 두고, 작은 보조 모션으로 정적인 체류를 완화한다.

`--motion` 검사는 **설명상 움직여야 할 대상이 실제로 움직이는가**를 찾는 보조 도구다.
모든 상태에 무한 애니메이션을 배치하는 완성도 점수가 아니다. 정지 화면은 이해할 대상이
분명하고 대본이 짧다면 정상이다. 고칠 곳은 대본이 반복·경로·대기를 말하는데 그 시간성이
화면에 없거나, 이미 읽은 화면을 오래 붙잡는 상태다.

### 어디에 다는가

**이미 화면에 서 있는 것 중, 지금 말이 닿아 있는 것**에만 단다. 화면에 새 설명판을
덮기보다 그 대상 안에 체크·꺾쇠·선 강조를 붙인다. 대부분의 컴포넌트는 이미 그 표시를
들고 있다 — `data-live` · `data-focus` · `data-picked` · `data-on`.

순차 공개 목록에서 "지금 말하는 항목"은 선택자로 잡는다.

```css
.list > .step[data-on="true"]:has(+ .step[data-on="false"]),  /* 아직 남았으면 마지막으로 열린 것 */
.list > .step:last-child[data-on="true"]                      /* 다 열렸으면 마지막 것 */
```

아직 아무것도 안 켜진 **첫 비트**에는 이 방식이 안 붙는다. 그 자리는 배경의 빛 한
겹(`course-still-light`)이 받는다 — 모든 화면에 깔려 있고, 본문보다 뒤에 있어서
정보와 경쟁하지 않는다.

### 초점과 강조를 같이 건다

같은 그림을 다시 쓰는 **회수 장표**(그림을 통째로 세워 두고 스텝마다 그중 한 부분을
이야기하는 화면)에서는 초점 하나로도 안 되고 강조 하나로도 안 된다.

- **초점만 걸면 정지 사진이 된다.** 초점 규칙은 대개 안 쓰는 것을 물리면서 앰비언트
  모션까지 같이 끈다 — 죽은 경로 위에서 도는 점이 시선을 끌기 때문이다. 그러면 앞 장에서
  살아 있던 그림이 회수 장표에서 멈추고, 장을 넘길 때 밝기까지 튄다.
- **강조만 걸면 산만하다.** 원래 돌던 것 여섯에 새 움직임이 하나 더해질 뿐이다.
- **둘을 같이 건다.** 초점이 안 쓰는 것을 물리고, 강조가 남은 곳 하나를 움직인다.
  초점 규칙이 모션을 끄는 구현이면 **강조 요소만 그 규칙에서 빼 준다.**

규칙 넷:

1. **주된 시선 유도는 하나다.** 넷을 읽혀야 하면 한 항목씩 짚는다. 같은 역할을 가진
   모듈의 작은 점들은 시작 위상만 달리해 호흡할 수 있고, 이 보조 발광을 네 곳의 동시
   설명으로 세지 않는다.
2. **큰 것은 자기가 움직인다.** 테두리·링처럼 제일 큰 요소는 점을 얹는 것보다 **자기
   점선을 흐르게 하는** 쪽이 크게 읽히고, 도는 것이 안 는다.
3. **강조의 색은 대상의 색 언어를 따른다.** 사람 승인이 금색이면 거기서 멈추는 점도
   금색이다. 색이 갈리면 두 개를 보게 된다.
4. **맥박은 씬 안에서 한 주기로.** 주기가 다르면 그 자체가 소음이다.

읽기와 쓰기처럼 **방향이 둘인 관계**는 설명 순서대로 진행한다. 필요하면 스텝을 나누고,
한 스텝 안에서는 요청 뒤에 응답이 오도록 시차를 둔다. 같은 물리적 경로를 왕복하면
배경선은 하나만 두고 이동 표식의 방향을 바꾼다.

### 세 가지 어휘

| | 뜻 |
| --- | --- |
| 호흡 | 빛과 밝기만. 진폭 작게. "살아 있다" |
| 흐름 | 신호가 경로를 지나간다. "지금 흐르는 중" |
| 대기 | 정해지지 않은 것이 흔들린다. "아직 비어 있다" |

주기는 이동 거리, 화면에서의 크기와 대비, 설명 박자를 함께 보고 정한다. 같은 발광은
보조층으로 재사용할 수 있지만 모든 씬의 주된 연출을 대신하지 않는다. **대본의 문법을
따른다** — "같은 입력을 두 번 돌렸다"를 말하면 두 결과가 **번갈아** 켜진다(둘이 동시에
서 있으면 그건 비교표지 두 번 돌린 그림이 아니다).

### 긴 설명의 정지감을 줄이는 기법

**지금 보고 있는 대상이 설명을 따라 변하도록** 만든다. 모든 정지 스텝을 움직이는 작업은
하지 않는다. 기존 3D 공간·색상·카메라·사례의 연속성을 먼저 살린다.

| 설명 상황 | 기존 대상 안에서 할 일 | 남겨야 할 것 |
| --- | --- | --- |
| 값을 계산하거나 조건을 적용 | 같은 가격 행에서 연간 → 나누기 → 월 환산, 같은 조건 칸에서 기준 → 값 대입 → 충족 여부 | 최종 값과 판정. 숫자의 원래 자리 |
| 문서·작업을 이어서 사용 | 같은 문서에 입력·출처를 채우고, 같은 작업 번호로 재개 | 앞뒤가 같은 대상이라는 공간적 연결 |
| 항목을 비교·검토 | 현재 항목에 꺾쇠·문장 강조를 함께 주고 다음으로 이동 | 한 번에 한 곳을 읽는 순서 |
| 확인이 끝남 | 확인 대상 바로 옆에 체크를 한 번 그려 남김 | 완료 표시. 확인 작업 자체를 반복하지 않음 |
| 수치·그래프를 오래 설명 | 숫자는 고정하고 비교 화살표만 작게 이동 | 수치·곡선 좌표·비교 조건 |
| 관계를 따라 자료가 이동 | 연결선을 따라 표식이 이동하고, 앞 결과가 와야 후속 작업이 시작 | 대기·반환·승인 경계 |
| 큰 상태 변화가 없음 | 현재 모듈의 작은 점이나 관련 선의 밝기를 낮은 진폭으로 반복 | 본문보다 뒤에 있는 보조 효과 |

- **의미 동작과 읽기 보조를 구분한다.** 계산·전달·승인 확인은 한 번 진행하고 결과를
  남긴다. 반복 강조를 추가 실행·승인·성과처럼 보이게 하지 않는다.
- **효과의 범위는 실제 대상에 맞춘다.** 숫자 아래 빛을 넓은 `div` 전체로 흘려 빈 공간까지
  훑지 않는다.
- **늦게 봐도 인지되는지 확인한다.** 작은 점이 한 번 반짝이고 없어지면 긴 설명에서는
  놓치기 쉽다. 읽기 보조라면 표식과 문장 강조를 묶어 순환시키고, 완료 표시라면 체크처럼
  결과를 남긴다.
- **줌 왕복만으로 진행을 대신하지 않는다.** 고정 구도에서도 값·조건·경로가 변하면 설명이
  진행된다. 카메라를 쓸 때는 제목·이름표와 부딪히지 않는 도착 구도로 연결한다.
- **속도는 보이는 움직임으로 판단한다.** 몇 px 움직이는 기호와 긴 경로를 같은 초로 맞추지
  않는다. 조건 읽기·요청과 반환·파일 펼침의 순서는 보존한다.

### 상태 변화와 스텝 내부 큐

독립적으로 설명할 수 있는 항목을 차례로 공개할 때는 **step과 대본을 먼저 나눈다.**
여러 문장을 한 step에 묶고 고정 지연으로 한 줄씩 여는 것을 기본 해법으로 삼지 않는다.
step 내부 모션은 같은 설명 안에서 이어지는 이동·변형·계산처럼 연속 동작이 필요한 곳에 쓴다.

각 클릭에서 실제로 달라진 대상을 확인한다. 코드의 뜻을 해설하는 한 줄만 추가했다면
그 짧은 해설에 맞는 대본을 붙인다. 원문 조건과 적용 사례, 입력과 반환, 서로 다른 시험
항목을 한 번에 공개한 뒤 긴 대본으로 설명하지 않는다. 개요에서 다음 항목의 내용을 먼저
읽히고, 다음 step에서 다시 숨겨 순차 공개하는 역행도 피한다.

같은 원문을 계속 읽는 장면에서는 원문 전체를 보존하면서 강조 범위와 옆 해설을 같이
옮긴다. 흐린 줄에 테두리만 덧씌워 읽을 수 없는 상태를 초점 변화로 세지 않는다.
모션 끄기·동작 줄이기·역방향 이동은 정보 공개 순서를 바꾸지 않아야 한다.

기존 요소의 위치·크기가 `data-*`로 바뀌면 그 변화는 `transition`으로 받는다. 같은
`animation` 선언은 속성만 바뀌었다고 자동으로 다시 시작하지 않는다. 한 스텝 안의 계산·
읽기 큐는 별도의 자식에서 재시작해 원래 문서와 월드의 DOM을 유지한다. 전체 장면을 다시
마운트해 카메라나 연결선을 끊지 않는다.

더 나쁜 경우는 조용히 덮는 것이다. 같은 속성에 `animation`이 걸려 있으면 그 값이 이기므로
`transform` 선언이 통째로 무시된다 — 계산값을 찍어 보면 의도한 `scale(2.7)`이 아니라
앰비언트의 `scale(1.001)`이 남아 있다. 화면도 빌드도 아무 말을 안 한다.

- 등장·앰비언트와 시작/종료가 명시된 스텝 내부 큐에 `animation`을 쓴다.
- 스텝이 바꾸는 속성에는 반드시 `transition`을 선언한다.
- 그 속성에 앰비언트가 이미 돌고 있으면 그 상태에서 `animation: none`으로 끈다.
- 안 될 때는 렌더 이미지를 보지 말고 **계산된 값을 읽는다.**

### 자리를 지키면서 연다

칸을 빼면 남은 칸이 밀리고, 다음 스텝에 도로 들어올 때 그림 전체가 튄다. 격자·절대 배치
안에서는 `display`나 mount로 빼지 말고 자리를 둔 채 `opacity`로만 연다. 묶음은
`--reveal-index`로 시차를 준다 — 넷이 동시에 튀어나오면 대본이 하나씩 부르는 동안 화면은
이미 끝나 있다. **도형에서 칸을 빼면 거기 붙은 선도 같이 빠져야 한다.** 안 그러면 아무
데도 닿지 않는 선이 남는다.

### 하면 안 되는 것 셋

1. **`opacity`를 펄스에 쓰지 않는다.** 대부분의 요소가 `data-on`이 바뀔 때 opacity로
   등장한다. 같은 속성에 애니메이션이 돌고 있으면 그 transition이 무시되고 요소가 툭
   나타나서 순차 공개가 통째로 거칠어진다. `filter: brightness` · `box-shadow` ·
   `text-shadow`를 쓴다. 부모가 등장을 맡는 자식에서만 opacity를 쓴다.
2. **레이아웃을 바꾸는 속성(width · height · margin)을 쓰지 않는다.** `shots --audit`의
   넘침·겹침 판정이 애니메이션 위상을 타게 된다. 크기를 바꿔야 하면 `transform: scale`.
3. **뜻을 들고 있는 속성을 덮지 않는다.** 고른 칸의 테두리색, 켜진 줄의 바탕처럼 상태를
   말하는 속성에 keyframe을 걸면 박자마다 정보가 지워진다. 그런 요소는 밝기만 오르내리게 한다.

선택적 모션은 `.deck-course[data-motion="on"]` 아래에 두고 `data-course-motion="off"`와
OS 동작 줄이기도 따른다. 껐을 때는 최종 값과 완료 상태가 읽혀야 한다.

### 재는 법

```bash
npm run shots -- --motion ch01
```

장표마다 스텝별로 **끝나지 않는 애니메이션이 몇 개 도는지** 센다. `·`는 그 상태에 씬
움직임이 하나도 없다는 뜻이다(배경의 빛은 세지 않는다 — 그건 바닥이지 연출이 아니다).
`·`는 결함이 아니다. 음성이 반복·흐름·대기를 말하는 자리만 우선 검토한다. 한 줄 결론·
정의·생각 시간은 정지 상태가 더 낫다.

CSS에 `animation`이 있거나 재생 시간이 바뀌었다는 사실만으로 확인을 끝내지 않는다.
정상 속도에서 시작·중간·완료 후의 실제 표시를 보고, 화면 안에 보이는지, 늦게 보면 사라져
있는지, 다른 정보와 겹치는지, 껐을 때도 읽히는지를 확인한다. 대표 상태 확인을 전수
점검으로 보고하지 않는다.

## 11. 리허설과 현장 반응으로 보정

화면 스타일은 플랫폼과 무관하게 이 문서의 연출 방향을 따른다. 리허설에서는
대본 분량(60분 안에 들어오는가)과 잠수 구간(청중의 눈이 떨어지는 장면)만 본다.
초반 5분이 약속한 문제·결과를 바로 보여주는지 확인하고, 지적된 반복 설명·난이도·
화면과 말의 불일치를 본다. 현장 질문이 몰린 자리는 다음 회의 설명을 늘리고,
아무도 묻지 않은 자리는 줄인다. 숫자를 직접 비교하지 않는다.
