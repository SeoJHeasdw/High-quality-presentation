# 다음 작업

2026-09-23 기준. [EXPANSION-PLAN.md](EXPANSION-PLAN.md)의 세션 1~3을 한 번에 진행했다(38장·69단계 → 43장·84단계).

## 현재 상태

- 새로 넣은 장: 14 과도기(13번과 묶음 person-ruler), 18 공장 안·19 베팅(16~19번 한 공간, phase 0~9), 29 가장 구린 모델(2~3번 입자 회수), 39 신에게 목줄.
- 다시 지은 장: 42 계속 늘어나는 방(새벽 하늘 위의 투명 3D 설계도, 세 단계, RICE 영상).
- 번호가 밀린 곳(화면의 "26번에서", "이 발표 · 43장", 대본의 "26번에서"·"17번에서", 검사 스크립트, README·설계 문서·React Bits 문서·FACT-CHECK)은 모두 고쳤다.
  검사 스크립트는 장표를 id로 찾도록 바꿔 다음 재배치에도 깨지지 않는다. Blender 영상 장면의 클릭 효과 제외도 번호가 아니라 장면으로 가린다.
- 사실 확인(F1~F16)은 원문을 확인해 `deck/FACT-CHECK.md` 마지막 표에 남겼다. 영수증은 사이드바를 잘라낸 `deck/public/rent/`만 쓰고 원본은 커밋하지 않는다.
- 발표자 나이는 31세(1995년생), 13번 눈금은 31세 → 61세.

## 이번 세션 마무리에서 정한 것

- 영수증 원본 두 장은 `.gitignore`에 넣고, 아직 푸시하지 않은 커밋에서 뺐다(399c8b0 → 213251c, 차이는 두 PNG와 `.gitignore`뿐). 원본 파일은 루트에 그대로 있다.
- 나이는 모든 곳에서 31세다. 기존 강의 쪽(`deck/src/production/chapters/ch01-open.ts`, `deck/script/course/ch06.md`)과 `강의안_통합.md`도 고쳤다(32세 → 31세, 62세 → 61세).
- "개발 중"·"만듦" 표시는 사용자에게 중요하지 않다. 발표는 완성을 보여주는 자리가 아니므로 지금 표시를 그대로 둔다.
- 확장 작업(38 → 43장)은 아직 커밋하지 않았다. 사용자가 요청할 때 커밋한다.
- 새 장의 문구(14번 "쾌락", 29번 "가장 구린", 39번 질문)는 리허설에서 발표자 말투에 맞춰 다듬는다.

## 유지할 것

- 21~25번 Blender 영상은 사용자 승인 완료. 변경하지 않는다.
- 발표 서버: `http://localhost:5180/#keynote`. 숫자 + Enter로 이동한다.
- 화면에 "에이전트가 만들었다"류 문구를 넣지 않는다. 유료 종목 토론방·데이터 판매·수익화 계획·거래내역은 무대에서 다루지 않는다.
